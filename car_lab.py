class Car(object):
  speed = 0
 
  
  def __init__(self, name = 'General', model = 'GM', body_type = 'saloon'):
    self.name = name
    self.model = model
    self.body_type = body_type
    
  def drive(speed,body_type):
    pass
  
  if self.name in ['porsche', 
  
  data = [name,model,body_type]
  if  not isinstance(d in data , str):
    raise ValueError("Invalid input on Car Creation")